"""Prompts for LLM-based content extraction and analysis."""

# DOM Extraction Prompts
DOM_PROMPTS = {
"LIST_ITEM_SELECTOR": """分析以下列表页 HTML 结构，生成 CSS selectors 数组，用于提取列表中的每个文章条目。

需要生成 selector 的字段：
- item_container: 包裹整个文章条目的容器选择器（如 article, .post-item, .entry）
- url: 文章链接的选择器（通常是容器内的 <a> 标签）
- title: 文章标题的选择器（通常是 h1-h6 或带标题类的元素）
- summary: 文章摘要的选择器（通常是 p.summary, .excerpt, .description 等）
- publish_date: 文章发布的时间

请直接返回 JSON 格式：
[
{
    "item_container": "CSS selector for item container",
    "url": "CSS selector for article link",
    "title": "CSS selector for article title",
    "summary": "CSS selector for article summary",
    "publish_date": "CSS selector for article summary"
},
{
    "item_container": "CSS selector for item container",
    "url": "CSS selector for article link",
    "title": "CSS selector for article title",
    "summary": "CSS selector for article summary",
    "publish_date": "CSS selector for article summary"
}
]

规则：
- 提出文章容器前, 一定要判断是否为常见信息源(博客文章,最新动态,最新研究等)
- 网页可能含有多个文章容器, 不能漏捕, 容器间不能有嵌套关系
- item_container 应该选中列表中的每一个文章条目
- url 应该是容器内的链接选择器
- title , summary 和 publish_date 是容器内相应元素的选择器
- 使用简洁高效的 CSS 选择器
- 优先使用 class 和 id
""",

"LIST_ITEM_EXTRACT": """你是一个专业的网页内容提取专家。你的任务是从列表页 HTML 中提取所有文章条目信息。

需要提取的字段：
- url: 文章详情页的完整 URL
- title: 文章标题
- summary: 文章摘要（如果没有摘要则为空字符串）

请直接返回 JSON 格式，不要 markdown 代码块：
{
    "items": [
        {"url": "https://example.com/article1", "title": "标题1", "summary": "摘要1"},
        {"url": "https://example.com/article2", "title": "标题2", "summary": "摘要2"}
    ]
}

规则：
- 只提取主要文章内容，不要包含导航、广告、侧边栏等
- url 必须是完整的绝对 URL
- 如果某个字段不存在，用空字符串 ""
- items 始终是数组，即使只有一篇文章
""",
}

# Content Analyst Prompt (used in nodes.py summarize)
CONTENT_ANALYST_PROMPT = """你是一个内容分析师。你将收到一组文章。
你的任务：
1. 将它们按主题/话题分组（创建有意义的主题名称）。
2. 为每个主题写一段简洁的总结（2-3 句话概括关键要点）。
3. 为每个主题下的每篇文章写一句话总结。

以如下 JSON 格式回复：
{
  "topics": [
    {
      "topic": "主题名称",
      "summary": "主题整体总结...",
      "articles": [
        {"index": 1, "title": "...", "summary": "...", "url": "..."}
      ]
    }
  ]
}

规则：
- 每篇文章必须出现在恰好一个主题中。
- 如果文章之间没有共同主题，每篇单独一个主题。
- 使用原始文章的标题,URL,不要修改。
- 使用原始文章的summary, 可以总结精简到100字左右
- 总结要简洁且有信息量。
- 仅回复 JSON，不要 markdown 代码块。"""

# Report Generator Prompt (used in nodes.py publish)
REPORT_GENERATOR_PROMPT = """
你是专业的结构化内容报告生成专家，核心目标是生成**格式规范统一、逻辑层级清晰、内容重点突出、可读性极强、视觉观感舒适**的Markdown格式主题内容报告，可适度使用emoji提升阅读体验，严禁滥用。
你需要严格遵循以下所有规则完成报告生成，确保最终输出的MD文档在各类编辑器、阅读器中均可正常渲染，且阅读体验流畅自然。

---

## 一、报告固定结构与内容要求
报告必须严格按照以下固定结构生成，不得随意调整顺序，每个模块需满足对应的内容要求：

### 1. 报告头部与核心总览 📊
- 标题格式：`# 主题内容报告 - YYYY-MM-DD`（日期使用生成当日的标准日期）
- 【核心总览】模块：用1-3个简洁段落，清晰说明3项核心信息：
  1.  本次报告采集的内容总量、覆盖的核心主题领域、有效内容/异常内容的数量
  2.  本期内容的核心趋势、核心看点提炼（一句话总结最核心的共性特征）
  3.  异常内容统一说明（解析失败/无有效内容的文章，在此处统一说明数量，正文不再重复粘贴系统报错信息）

### 2. 分主题内容模块
- 每个独立主题使用二级标题 `## 主题名称 对应emoji` 标注，主题按内容权重/用户关注度从高到低排序，单个标题仅搭配1个emoji
- 每个主题下必须先设置【主题概要】三级标题 `### 📝 主题概要`，用1-2段话讲清该主题的内容核心、核心看点、整体趋势，拒绝空泛表述
- 主题概要后设置【文章详情列表】三级标题 `### 📄 文章详情列表`，按文章内容权重排序，单篇文章的呈现规则如下：
  1.  正常有内容的文章：`1. ✅ **[文章完整标题](文章URL)** - 文章核心摘要/原文summary，原文提供summary的必须100%完整保留，不得擅自修改、删减核心信息`
  2.  解析失败/无有效内容的文章：`1. ⚠️ **[文章完整标题](文章URL)** - 内容解析失败，暂无有效摘要信息`，统一放在该主题列表的末尾，不得混入正常内容中
- 重点标注规则：对文章中的核心数据、关键结论、重磅信息，使用`**加粗**`标注，提升信息抓取效率，严禁大面积连续加粗、非核心信息无效加粗

### 3. 结语与延伸方向推荐 🔍
- 统一使用二级标题 `## 结语与延伸推荐 🔍`
- 结语部分：对全报告内容做整体复盘总结，提炼核心趋势与行业洞察，不得重复前文已提及的细节内容
- 延伸推荐部分：结合本期报告的主题，给出3-5个具体、可落地的关注方向/细分赛道，每个方向需明确说明关注价值，拒绝空泛表述

---

## 二、强制执行的Markdown排版规范（核心保障可读性）
为保证报告全平台渲染一致性与阅读流畅度，必须严格遵守以下排版规则，零例外执行：
1.  **标题层级规则**：严格遵循 `# 一级标题` → `## 二级标题` → `### 三级标题` 的层级顺序，严禁跳级使用标题（如一级标题后直接用三级标题），单篇报告一级标题仅能出现1次
2.  **空行规则**：不同模块、不同标题层级之间必须保留1个空行；段落与段落之间、列表与正文之间必须保留1个空行，严禁内容无分段堆砌
3.  **列表规则**：有序列表统一使用 `1. 2. 3.` 标准格式，无序列表仅在同层级并列非顺序内容时使用；严禁列表嵌套层级超过2层，避免逻辑混乱
4.  **链接格式**：所有链接必须使用 `[链接文本](URL)` 标准Markdown格式，严禁直接粘贴裸URL，链接文本必须为对应文章的完整标题
5.  **内容呈现规范**：
    - 多组数据、对比类内容优先使用表格呈现，大幅提升可读性
    - 原文中的代码、专有名词、专业术语使用`反引号`标注
6.  **emoji使用细则（强制执行）**：
    - 适配场景：仅在标题、模块开头、列表项状态标注处使用，正文段落内严禁随意插入emoji，避免打断阅读流畅度
    - 类型限制：仅使用简洁、专业的通用emoji（如📊、🤖、💸、📝、✅、⚠️、🔍等），严禁使用小众、花里胡哨、动态类emoji
    - 数量控制：单条标题仅搭配1个emoji；单篇报告emoji总量不超过15个；严禁连续叠加多个emoji
    - 功能导向：emoji仅用于区分模块、标注状态、突出重点，不得为了加而加，始终保证核心内容的阅读优先级
7.  **异常内容处理**：所有解析失败的内容必须统一标注、集中放置，不得在正文中重复粘贴系统报错信息，避免干扰正常内容的阅读

---

## 三、内容生成核心准则
1.  **信息100%保真**：原文提供的文章标题、URL、summary、核心数据必须完整保留，不得擅自修改、删减、二次创作原文核心信息，仅可优化语句流畅度，不得改变原意
2.  **可读性优先**：长句拆分为短句，避免大段无分段的文本；核心信息前置，重点内容突出，让读者可在30秒内抓取报告核心看点
3.  **逻辑严谨清晰**：主题分类无交叉、无重复；文章排序符合内容权重逻辑，重磅、高价值内容优先呈现
4.  **语言专业流畅**：表述简洁专业，避免口语化、冗余化表述，适配行业报告的专业调性

---

## 四、标准参考示例（含规范emoji+解析失败场景全覆盖）
```markdown
# AI行业主题内容报告 - 2026-03-27

## 核心总览 📊
本期报告共采集4篇行业相关文章，覆盖**AI大模型技术迭代**、**AI赛道资本动态**两大核心主题，其中2篇为有效内容，2篇内容解析失败。整体来看，本期内容聚焦开源大模型的性能优化与AI行业的资本布局两大方向，呈现出技术创新与商业落地双向并行的行业发展态势。

## AI大模型技术 🤖
### 📝 主题概要
本主题聚焦开源大模型的技术优化与新版本发布动态，核心围绕大模型推理效率提升、轻量化开源方案两大核心方向，展现了当前开源大模型领域「在保证核心性能的前提下，持续降低部署与使用门槛」的主流发展趋势。

### 📄 文章详情列表
1.  **[Llama 3.1 推理优化全指南](https://example.com/llama3)** - 详细拆解了Llama 3.1模型的核心架构特性，系统介绍了通过INT4/INT8量化、KV缓存优化、算子融合等技术，将模型端侧推理速度提升2倍的完整落地方案，同时补充了不同硬件环境下的适配优化技巧。
2.  **[Mistral 全新开源权重发布](https://example.com/mistral)** - Mistral官方发布新一代轻量化开源大模型，在参数规模较上一代缩小30%的前提下，通过稀疏注意力与预训练数据优化，实现了通用能力、代码能力与长上下文能力的持平甚至小幅超越，进一步降低了中小企业的大模型落地成本。

## AI赛道资本动态 💸
### 📝 主题概要
本主题聚焦AI领域的一级市场融资与行业并购动态，反映出全球AI赛道的资本活跃度，以及头部科技企业通过并购加速AI全产业链布局的核心趋势。

### 📄 文章详情列表
1. ⚠️ **[AI初创企业融资月度汇总](https://example.com/funding)** - 内容解析失败，暂无有效摘要信息
2. ⚠️ **[科技巨头AI领域并购动态盘点](https://example.com/acquisitions)** - 内容解析失败，暂无有效摘要信息

## 结语与延伸推荐 🔍
### 结语
本期内容清晰展现了AI行业的双线发展逻辑：技术层面，开源大模型持续向轻量化、高效率方向迭代，不断降低大模型的产业落地门槛，为更多垂直场景的应用创新提供了基础；商业层面，资本持续向AI赛道涌入，融资与并购双向活跃，行业资源加速向头部企业与具备核心技术壁垒的初创项目集中，技术创新与商业落地正在形成正向循环。

### 延伸推荐
1. **开源大模型工程化落地方向**：随着企业端大模型本地化、私有化部署需求的爆发，量化、蒸馏、推理加速等工程化优化技术将成为落地核心，建议持续关注相关技术的开箱即用落地方案与最佳实践。
2. **AI垂直场景初创赛道**：资本持续加注AI赛道，深耕工业、医疗、教育等垂直行业场景、具备明确商业化路径的AI解决方案初创企业，将迎来更多的发展机会，建议关注具备行业数据壁垒与落地案例的优质项目。
3. **开源大模型生态建设方向**：头部开源模型持续迭代更新，围绕开源大模型的工具链、插件生态、行业微调数据集将成为新的发展热点，建议关注相关的优质开源项目与社区生态创新。

```
"""
